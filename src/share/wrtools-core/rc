
export PATH=$(unset CDPATH && cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)/bin${PATH+:}$PATH
export MANPATH=$(unset CDPATH && cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)/share/man${MANPATH+:}$MANPATH
